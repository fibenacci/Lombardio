spring:
  application:
    name: customer-service
  datasource:
    url: ${CUSTOMER_DB_URL}
    username: ${CUSTOMER_DB_USERNAME}
    password: ${CUSTOMER_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: customer
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

server:
  port: 8084

kyc:
  base-url: ${KYC_BASE_URL}

identity-access:
  base-url: ${IDENTITY_ACCESS_BASE_URL}

pawn-ticket:
  base-url: ${PAWN_TICKET_BASE_URL:http://localhost:8085}

integration:
  base-url: ${INTEGRATION_BASE_URL:http://localhost:8092}

customer:
  internal-service-token: ${CUSTOMER_INTERNAL_SERVICE_TOKEN:dev-internal-token}

customer-portal:
  public-base-url: ${CUSTOMER_PORTAL_PUBLIC_BASE_URL:http://localhost:5173/portal}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS}
    allowed-methods: ${APP_CORS_ALLOWED_METHODS}
    allowed-headers: ${APP_CORS_ALLOWED_HEADERS}
    exposed-headers: ${APP_CORS_EXPOSED_HEADERS}
    max-age-seconds: ${APP_CORS_MAX_AGE_SECONDS}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
