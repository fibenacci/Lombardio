spring:
  application:
    name: aml-service
  datasource:
    url: ${AML_DB_URL}
    username: ${AML_DB_USERNAME}
    password: ${AML_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    default-schema: aml
    table: aml_flyway_schema_history
    create-schemas: true
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

server:
  port: 8088

identity-access:
  base-url: ${IDENTITY_ACCESS_BASE_URL}

platform:
  base-url: ${PLATFORM_BASE_URL}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS}
    allowed-methods: ${APP_CORS_ALLOWED_METHODS}
    allowed-headers: ${APP_CORS_ALLOWED_HEADERS}
    exposed-headers: ${APP_CORS_EXPOSED_HEADERS}
    max-age-seconds: ${APP_CORS_MAX_AGE_SECONDS}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
